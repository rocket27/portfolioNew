.box.box_contacts.contacts
  .box__content.contacts__inner
    .contacts__title
      .subtitle.subtitle_contacts Контакты
    ul.contacts__list
      each item in contacts
        li.contacts__item
          a.contacts__link(href=`${item.link}`)
            svg.contacts__icon
              use(xlink:href=`img/sprite.svg#${item.fileName}`)
            span.contacts__text= `${item.name}`